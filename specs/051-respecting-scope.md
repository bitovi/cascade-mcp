# 051 - Respecting Scope

## Generated Questions (21 total)

### Create New Case - Desktop (6 questions)
1. What validation occurs if required fields are left empty when clicking "Create Case"?
2. What options are available in the Priority dropdown beyond "Medium"?
3. What options are available in the Customer dropdown - is it a searchable list or fixed options?
4. Does the "Assigned To" dropdown show all users or only users with specific roles/permissions?
5. What happens when clicking "Cancel" - are there unsaved changes warnings?
6. How does the case ID generation work - is it automatic upon creation or preview?

### New Case Created - Desktop (5 questions)
1. When saving changes to the case description, does it auto-save or require explicit save action?
2. What options are available in the status dropdown beyond "To Do"?
3. Can users edit Essential Details fields inline, and if so, which fields are editable vs read-only?
4. What happens when clicking the three-dot menu button next to the status?
5. Is there a character limit for comments, and are there any content restrictions?

### Case Deleted (3 questions)
1. Why does the "Fraud Investigation" case still appear in the case list if it was successfully deleted according to the alert?
2. Should the user be redirected to a different case after deleting the currently viewed case?
3. What happens if a user tries to interact with case details (edit, comment) while the deletion alert is showing?

### Create New Case - Mobile (4 questions)
1. Do the dropdown fields (Customer, Priority, Assigned To) support search/filtering on mobile?
2. What happens when switching between Cases and Customers tabs - is form data preserved?
3. Are there different validation messages for mobile vs desktop when required fields are missing?
4. Does the textarea for Case Description have a character limit on mobile?

### New Case Created - Mobile (3 questions)
1. How does inline editing work on mobile - tap to edit individual fields or edit mode for entire form?
2. Can the Essential Details accordion be collapsed on mobile, and does it remember state?
3. What is the interaction pattern for the sidebar toggle button (hamburger menu)?
