/**
 * Shared TypeScript types for MCP (Model Context Protocol) tools
 * These types are imported directly from the MCP SDK to ensure compatibility
 */

export type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
